#include <stdio.h>

int main(void) {
    /* TODO: leia dois numeros e a opcao desejada. */

    /* TODO: use switch para executar a operacao escolhida.
       Na divisao, verifique antes se o segundo numero e diferente de zero. */

    return 0;
}
